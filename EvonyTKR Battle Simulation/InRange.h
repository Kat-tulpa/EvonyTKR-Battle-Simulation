#pragma once
#include "InitialStats.h"

class InRange {
private:

public:

	static const bool defenderSiegeToAttackerSiege(
		const unsigned int attackerTier, const unsigned int defenderTier)
		// If They Haven't Moved Past Each Other Already
		if (!position.hasCrossed(TYPE_SIEGE, attackerTier, TYPE_SIEGE, defenderTier))
			if (position.)
		if (position.)
	}

	static const bool attackerSiegeToDefenderSiege(const Type defenderType, 
		const unsigned int defenderTier) {

	}

	static const bool attackerToDefender(const Type attackerType, const Type defenderType) {
		if ()
	}

} inRange;