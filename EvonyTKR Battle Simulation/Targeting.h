#pragma once
#include "StartingStats.h"

static bool defenderSiegeToAttackerSiege(int attackerTier, int defenderTier)
	// If They Haven't Moved Past Each Other Already
	if (!position.hasCrossed(TYPE_SIEGE, attackerTier, TYPE_SIEGE, defenderTier))
		if (position.)
	if (position.)
}

static bool attackerSiegeToDefenderSiege(const Type defenderType, 
	const unsigned int defenderTier) {

}

static bool attackerToDefender(const Type attackerType, const Type defenderType) {
	if ()
}